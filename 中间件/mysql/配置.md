```sh
docker run -d --name mysql -p 3306:3306 -v /opt/usr/mysql/conf.d/:/etc/mysql/conf.d  -v /opt/usr/mysql/data/:/var/lib/mysql -e MYSQL_ROOT_PASSWORD=root  mysql:8.0
```

```sql
update user set host = '%' where user = 'root';
flush privileges;
```

```sh
netstat -ntlp | grep 3306
```